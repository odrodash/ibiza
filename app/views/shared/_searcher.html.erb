<%= simple_form_for :product, url: search_path, method: :get do |f| %>
<%= f.input :name, label: false, placeholder: 'Ingresa un producto' %>
<%= f.submit "Buscar", class: "btn btn-primary btn-block", id:"home-search" %>
<% end %>
